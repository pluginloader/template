pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
        maven{url "https://repo.implario.dev/public"}
    }
}

rootProject.name = 'template'
